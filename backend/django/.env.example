# Copy to .env and fill in real values. Never commit the real .env file.

# Infisical Vault (optional). When INFISICAL_CLIENT_ID/SECRET are set, Django
# sources SECRET_KEY, DATABASE_URL, and REDIS_URL from Vault first, falling
# back to the plain values below if Vault is unreachable, unconfigured, or
# missing a given key. Leave the two below blank to skip Vault entirely and
# use only the fallback values (today's behavior).
INFISICAL_URL=http://localhost:8081
INFISICAL_CLIENT_ID=
INFISICAL_CLIENT_SECRET=
INFISICAL_PROJECT_ID=
INFISICAL_ENVIRONMENT=dev
INFISICAL_SECRET_PATH=/

# Fallback values, used directly if Vault is unconfigured/unreachable/missing a key.
SECRET_KEY=change-me-to-a-random-50-char-secret
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# postgres://USER:PASSWORD@HOST:PORT/NAME
DATABASE_URL=postgres://djaskt:djaskt@localhost:5432/djaskt_ledger

REDIS_URL=redis://localhost:6379/0

# Production-only hardening (leave off for local dev)
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False
